export enum QuizCategory {
  Angular = 'Angular',
  Design_Patterns = 'Design patterns',
  Java = 'Java',
}

export enum Page {
  Questions = 'questions',
  Results = 'results',
}

export enum Direction {
  Prev = 'prev',
  Next = 'next',
}
